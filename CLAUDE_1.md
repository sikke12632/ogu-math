# 프로젝트 규칙

## 이 프로젝트

초등 5학년 수학 수업용 웹앱. 반 전체가 크롬북으로 동시 접속해
같은 문제를 풀고, 점수로 팀을 나눠 1대1 게임을 한다.
만드는 사람은 초등학교 교사이며, 학기 중에 직접 운영한다.

설계 근거는 전부 `docs/` 에 있다. 작업 전에 읽어라.

- `docs/수학_퀴즈배틀_설계보고서.md` — 문제 생성기, 게임 규칙, 난이도 근거
- `docs/사이트_아키텍처_설계서.md` — 화면 구조, 라우팅, 확장 계획
- `docs/할일_목록.md` — STEP 0~10. 지금 어느 단계인지 확인할 것
- `docs/수직선_렌더러_검증.html` — 수직선 SVG 렌더러 참고 구현

## 작업 범위

**한 세션에 한 STEP만 한다.** 지시받은 STEP 밖의 파일은 건드리지 마라.
"이왕 하는 김에" 다음 단계를 미리 만들지 마라. 검수 시점을 놓치게 된다.

지시가 STEP을 명시하지 않으면, 진행하기 전에 어느 STEP인지 물어봐라.

## 보고 의무

작업이 끝나면 반드시 다음을 보고한다.

1. **임의로 정한 것 전부** — 설계 문서에 없어서 네가 결정한 모든 것.
   라이브러리 선택, 파일 이름, 함수 시그니처, 기본값, 폴더 구조 등.
2. **설계 문서와 어긋난 부분** — 문서대로 하면 안 되는 이유가 있었다면 그 이유와 함께.
3. **다음 STEP으로 넘어가기 전에 사람이 확인해야 할 것.**

보고 없이 넘어가지 마라. 나중에 원인을 못 찾는다.

## 설명 방식

만든 사람은 개발자가 아니다. 코드는 읽지 않고 결과로 판단한다.

- 무엇을 만들었는지 **평범한 말로** 설명해라. 전문 용어를 쓸 거면 그 자리에서 한 줄로 풀어라
- "동작합니다" 대신 **어떻게 확인하는지** 알려줘라. 실행할 명령, 열어볼 주소, 눌러볼 버튼
- 에러가 나면 원인을 추측하지 말고, 확인할 방법을 먼저 제시해라

## 코드 규칙

- **TypeScript.** `any` 금지. 타입이 안 잡히면 그 자체가 설계 문제 신호다
- **`src/units/` 는 Firebase를 몰라야 한다.** 단독 실행해서 문항을 뽑아볼 수 있어야 검수가 가능하다
- **단원·게임 추가가 코어 코드 수정을 요구하면 멈춰라.** 인터페이스가 잘못된 것이다. 보고하고 지시를 기다려라
- **`Problem.explanation` 은 필수다.** 게임 후 오답 해설에 쓰인다. 비워 두지 마라
- 브라우저 저장은 `localStorage` 만 쓴다 (학생 이름, 답안 미러링). 그 외 상태는 서버가 단일 소스
- `phase` 는 교사 클라이언트만 변경한다. 학생·보드 화면은 읽기만

## 학생이 보는 문구

- 교사용 해설 어휘를 학생 화면에 쓰지 마라. 예: "경곗값" (교과서 발문에 없다)
- 교과서에 실제로 나오는 말만 쓴다: 이상, 이하, 초과, 미만, 속하는 수, 나타낸 것
- 발문은 2줄 이내. 문항당 40~50초 안에 읽고 풀 수 있어야 한다

## 문제 은행

- 시중 문항을 복제하지 마라. 수치만 바꾸는 것도 복제다
- `docs/` 의 유형 분석 결과만 근거로 삼아 문항을 새로 작성한다
- 생성한 문항은 반드시 자동 검사를 통과해야 한다 (가드 위반, 정답 유일성, 보기 중복, NaN)

## 배포

GitHub Pages. 학교 크롬북에서 GitHub 도메인이 열리는 것을 확인했다.
`vite.config.ts` 의 `base` 를 리포 이름에 맞춰 설정할 것.
